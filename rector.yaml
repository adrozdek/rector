parameters:
    exclude_paths:
        # autoload-buggy cases
        - "*.php.inc"

    # so Rector code is still PHP 7.1 compatible
    php_version_features: '7.1'

services:
    Rector\TypeDeclaration\Rector\Property\FixVarDocTypePropertyRector: ~
